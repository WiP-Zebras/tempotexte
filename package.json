{
  "name": "tempotexte",
  "version": "0.1.0",
  "private": true,
  "description": "Estimateur statique de temps de lecture",
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 4173 --directory docs",
    "test": "node --test",
    "build": "node scripts/build.mjs"
  }
}
